# Additional DB Configurations
# ===
#spring.jpa.hibernate.ddl-auto=update
#logging.level.root=ERROR

# Authentication Configuration (Clerk)
# ===
#auth.validator.type=clerk

# Prod
#clerk.jwks.url=${CLERK_JWKS_URL:https://usable-parrot-49.clerk.accounts.dev/.well-known/jwks.json}

# CORS set to your Vercel frontend URL on Render once deployed
# e.g. CORS_ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3001
#cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3001}



spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.hyydbvvgmkeznpuujrgo}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update

clerk.jwks.url=${CLERK_JWKS_URL}

spring.datasource.hikari.data-source-properties.sslmode=require

#supabase password:AEIYI05nc8KzJdKt